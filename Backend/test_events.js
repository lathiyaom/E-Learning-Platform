require('mongoose').connect('mongodb://127.0.0.1:27017/e-learning').then(async () => { const events = await require('./src/models/Event.mongoose.js').find().lean(); const holidays = await require('./src/models/Holiday.mongoose.js').find().lean(); console.log('EVENTS:', JSON.stringify(events, null, 2).slice(0, 1000)); console.log('HOLIDAYS:', JSON.stringify(holidays, null, 2).slice(0, 1000)); process.exit(0); });
